<%= form_with(model: block_user, local: true) do |form| %>
  <% if block_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(block_user.errors.count, "error") %> prohibited this block_user from being saved:</h2>

      <ul>
        <% block_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :block_user_id %>
    <%= form.number_field :block_user_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
