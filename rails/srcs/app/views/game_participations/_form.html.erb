<%= form_with(model: game_participation, local: true) do |form| %>
  <% if game_participation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_participation.errors.count, "error") %> prohibited this game_participation from being saved:</h2>

      <ul>
        <% game_participation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score %>
  </div>

  <div class="field">
    <%= form.label :is_winner %>
    <%= form.check_box :is_winner %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
