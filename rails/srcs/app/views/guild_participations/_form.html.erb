<%= form_with(model: guild_participation, local: true) do |form| %>
  <% if guild_participation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guild_participation.errors.count, "error") %> prohibited this guild_participation from being saved:</h2>

      <ul>
        <% guild_participation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :guild_id %>
    <%= form.text_field :guild_id %>
  </div>

  <div class="field">
    <%= form.label :is_admin %>
    <%= form.check_box :is_admin %>
  </div>

  <div class="field">
    <%= form.label :is_officer %>
    <%= form.check_box :is_officer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
